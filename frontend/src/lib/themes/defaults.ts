/** Default theme constants — also used by Platform pages (always). */
import type { TenantTheme } from "./types";

export const DEFAULT_THEME: TenantTheme = {
  primaryColor: "#0A0A0A",
  secondaryColor: "#F2F2F0",
  accentColor: "#F97316",
  textOnPrimary: "#FFFFFF",
  textOnSecondary: "#0A0A0A",
};